<% if flash.any? %>
  <div class="container mt-3">
    <% flash.each do |type, message| %>
      <div class="alert <%= type == 'alert' ? 'alert-danger' : 'alert-info' %>" role="alert">
        <%= message %>
      </div>
    <% end %>
  </div>
<% end %>
