<style>
    /* Page-specific styles for email pending page */
    .card {
        max-width: 36rem;
        padding: 3rem 2.5rem;
    }

    .icon-container {
        width: 5rem;
        height: 5rem;
        margin: 0 auto 1.5rem;
        font-size: 2.5rem;
    }

    h1 {
        font-size: 2rem;
        margin-bottom: 0.75rem;
    }

    .email-sent {
        background: #f0f9ff;
        border: 1px solid #bae6fd;
        border-radius: 0.75rem;
        padding: 1.5rem;
        margin-bottom: 2rem;
        text-align: left;
    }

    .email-sent-title {
        font-weight: 600;
        font-size: 0.95rem;
        margin-bottom: 0.75rem;
        color: var(--text-primary);
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .email-sent p {
        font-size: 0.875rem;
        color: var(--text-secondary);
        line-height: 1.6;
        margin-bottom: 0.75rem;
    }

    .email-address {
        display: inline-block;
        background: #e0f2fe;
        color: #075985;
        padding: 0.5rem 1rem;
        border-radius: 0.5rem;
        font-weight: 600;
        font-size: 0.875rem;
        margin-top: 0.5rem;
    }

    .steps-box {
        background: var(--bg-surface);
        border: 2px dashed var(--border-color);
        border-radius: 0.75rem;
        padding: 1.5rem;
        margin-bottom: 2rem;
        text-align: left;
    }

    .steps-title {
        font-weight: 600;
        font-size: 0.95rem;
        margin-bottom: 1rem;
        color: var(--text-primary);
    }

    .steps {
        list-style: none;
        counter-reset: email-steps;
        display: grid;
        gap: 0.75rem;
    }

    .steps li {
        display: flex;
        gap: 0.75rem;
        align-items: flex-start;
        font-size: 0.875rem;
        color: var(--text-secondary);
    }

    .steps li::before {
        counter-increment: email-steps;
        content: counter(email-steps);
        width: 1.75rem;
        height: 1.75rem;
        border-radius: 50%;
        background: linear-gradient(135deg, var(--ibm-blue-60), var(--ibm-blue-50));
        color: white;
        font-size: 0.75rem;
        font-weight: 700;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }

    .info-box {
        background: #fef3c7;
        border-left: 4px solid #f59e0b;
        border-radius: 0.5rem;
        padding: 1rem 1.25rem;
        margin-bottom: 2rem;
        text-align: left;
    }

    .info-box p {
        margin: 0;
        font-size: 0.875rem;
        color: #92400e;
        line-height: 1.5;
    }

    .info-box strong {
        display: block;
        margin-bottom: 0.25rem;
        font-weight: 600;
    }

    .dev-link-box {
        background: #f0fdf4;
        border: 1px solid #bbf7d0;
        border-radius: 0.75rem;
        padding: 1.5rem;
        margin-bottom: 2rem;
        text-align: left;
    }

    .dev-link-title {
        font-weight: 600;
        font-size: 0.95rem;
        margin-bottom: 0.75rem;
        color: #166534;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .dev-link-box p {
        font-size: 0.875rem;
        color: #15803d;
        margin-bottom: 1rem;
    }

    @media (max-width: 640px) {
        .card {
            padding: 2rem 1.5rem;
        }
        
        h1 {
            font-size: 1.5rem;
        }
    }
</style>

<div class="card">
    <div class="icon-container">
        📧
    </div>

    <h1>Check your email</h1>
    <p class="subtitle">
        We've sent a verification link to confirm your email address and continue the signup process.
    </p>

    <div class="email-sent">
        <div class="email-sent-title">
            <span>✉️</span>
            <span>Email sent to:</span>
        </div>
        <p>Check your inbox (and spam folder) for an email from us.</p>
        <div class="email-address">
            <%= session[:facial_signup_email] %>
        </div>
    </div>

    <div class="steps-box">
        <div class="steps-title">Next steps:</div>
        <ol class="steps">
            <li>Open the verification email we just sent you</li>
            <li>Click the "Verify Email Address" button</li>
            <li>You'll be redirected to scan a QR code</li>
            <li>Complete facial scan on your mobile device</li>
        </ol>
    </div>

    <div class="info-box">
        <p>
            <strong>⏱️ Didn't receive the email?</strong>
            Check your spam folder or wait a few minutes. The link expires in 1 hour.
        </p>
    </div>

    <% if @verification_url.present? %>
        <div class="dev-link-box">
            <div class="dev-link-title">
                <span>🔗</span>
                <span>Development Mode - Direct Link</span>
            </div>
            <p>Click the button below to verify your email:</p>
            <%= link_to 'Verify Email Address →', @verification_url, class: 'btn btn-primary', style: 'display: inline-block;' %>
        </div>
    <% end %>

    <%= link_to new_user_registration_path, class: 'back-link' do %>
        <i class="bi bi-arrow-left"></i>
        <span>Back to signup form</span>
    <% end %>
</div>